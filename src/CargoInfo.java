public class CargoInfo extends Cargo {
    public CargoInfo(String sender, String receiving, int weight, int volume2, int cargonumber, String  time) {
        super(sender, receiving, weight, volume2, cargonumber, time);
    }
}
